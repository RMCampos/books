# Tasks: <spec-name>

Replace these items with tasks specific to the spec. Each task should be a concrete, verifiable action.

## Schema
- [ ] Define or update tables in `convex/schema.ts`
- [ ] Verify field types use proper `v.*` validators

## Convex Functions
- [ ] Write query function(s) with auth scoping
- [ ] Write mutation function(s) with auth scoping
- [ ] Write action function(s) if external API calls are needed

## Frontend
- [ ] Connect UI to queries via `useQuery`
- [ ] Connect UI to mutations via `useMutation`
- [ ] Handle loading and error states

## Validation
- [ ] Verify all acceptance criteria in `spec.md`
- [ ] Check Convex dashboard for correct document structure
- [ ] Confirm live reactivity works (open two tabs, mutate in one, see update in other)
